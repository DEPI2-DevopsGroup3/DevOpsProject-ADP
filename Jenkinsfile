pipeline {
    agent { 
        label 'local-agent' 
    }
    
    stages {
        stage('Build') {
            steps {
                echo "Build in progress"
            }
        }
        
        stage('Test') {
            steps {
                echo "Test in progress"
            }
        }
    }
}
